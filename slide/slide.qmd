---
title: "Let’s run Julia everywhere from mobile to web"
author: "terasakisatoshi"
format:
  revealjs:
    theme: dark
    slide-number: true
    preview-links: auto
    css: assets/slide-executor.css
    include-after-body: assets/after-body.html
---

## What I created

- SubsetJuliaVM (a.k.a `sjulia`)
  - Supports a subset of Julia's syntax
  - A stack-based VM (Virtual Machine) implemented in Rust
- Runs on iOS and Android devices
  - The VM compiles to WebAssembly, so it also runs in the browser
- Supports parts of Plots.jl, OrdinaryDiffEq.jl, AbstractAlgebra.jl, and more

## Deliverables {.smaller}

**iOS / iPadOS** — a native app with no JIT, fully App Store compliant

::: {.columns}
::: {.column width="33%"}
**iOS app**<br>[App Store](https://apps.apple.com/us/app/subsetjuliavm/id6757257182)

![](assets/ios-app.png){width="240"}
:::
::: {.column width="33%"}
**Source code**<br>[julia-vm-oss](https://github.com/AtelierArith/julia-vm-oss)

![](assets/julia-vm-oss.png){width="240"}
:::
::: {.column width="33%"}
**Web playground**<br>[subset_julia](https://terasakisatoshi.github.io/subset_julia/)

![](assets/web_playground.png){width="240"}
:::
:::

### Google it `SubsetJuliaVM`

## Why I created it

- I wanted to run Julia on my iPad.
  - **iOS / iPadOS** (no JIT allowed, strict App Store review)
  - W^X (Write XOR Execute) policy
- What about Binder or Google Colab?
  - Unusable where the internet connection is unreliable
  - Installing external packages takes a long time

## How I created it

- Apps like Pythonista and Carnets do run on iOS.
  - So why not build a virtual machine that accepts Julia syntax?!
- Let's actually build it!
  - A stack-based bytecode interpreter (**no JIT**)
  - `Julia source → Parser → Lowering → Compiler → VM`
  - Built with heavy use of AI tools: Claude Code, Codex, Cursor, Kimi, etc.
  - Result: [AtelierArith/julia-vm-oss](https://github.com/AtelierArith/julia-vm-oss)

## How sjulia differs from upstream Julia {.smaller}

How code is interpreted and executed:

| Stage | Upstream Julia | SubsetJuliaVM |
|------|-----------|---------------|
| Parser | flisp / JuliaSyntax.jl | pure Rust parser → CST |
| Lowering | lowered IR | Rust implementation → Core IR |
| Code generation | native code via LLVM **JIT** at runtime | bytecode compiler (**done before execution**) |
| Execution | native code | stack-based VM (bytecode interpretation) |
| Runtime | C runtime + pure Julia `base/` | Rust builtins + a pure Julia subset |

- With no JIT, SubsetJuliaVM **runs even on iOS (W^X) and WASM**

## Live demo (inside these slides)

- Seeing is believing
- The next slides embed **the very same WASM VM**.

Edit the code and run it with **Ctrl+Enter** / **⌘+Enter** (or **Run**). Press **Clear** to clear the output.

::: {.notes}
The demo is the heart of this talk. Keep the explanation short and run the code.
:::

## Demo · Hello, sjulia

```{=html}
<div class="sjulia-executor" data-code="println(&quot;Hello from WebAssembly!&quot;)\nprintln(1 + 2 * 3)"></div>
```

## Demo · Plotly via Plots

```{=html}
<div class="sjulia-executor" data-code="using Plots\nx = 0:0.1:2π\nplot(x, sin.(x), label=&quot;sin&quot;)\nplot!(x, cos.(x), label=&quot;cos&quot;)\ntitle!(&quot;Running in your browser&quot;)"></div>
```

## Demo · Mandelbrot heatmap

```{=html}
<div class="sjulia-executor" data-code="using Plots\nfunction kernel(c, n)\n  z = 0.0+0.0im\n  for k in 1:n\n    abs2(z) > 4 && return k\n    z = z^2 + c\n  end; n\nend\nxs = range(-2, 1; length=80)\nys = range(\n  -1.2, 1.2;length=60\n)\ncs = xs' .+ im .* ys\nzs = kernel.(cs, Ref(40))\nheatmap(xs, ys, zs)"></div>
```

## Demo · Lorenz attractor (OrdinaryDiffEq)

```{=html}
<div class="sjulia-executor" data-code="using OrdinaryDiffEq\nfunction lorenz!(du, u, p, t)\n    du[1] = 10.0 * (u[2] - u[1])\n    du[2] = u[1] * (28.0 - u[3]) - u[2]\n    du[3] = u[1] * u[2] - (8 / 3) * u[3]\nend\nu0 = [1.0, 0.0, 0.0]\ntspan = (0.0, 60.0)\nprob = ODEProblem(lorenz!, u0, tspan)\nsol = solve(prob, Tsit5(), dt=0.05, saveat=0.05)\nusing Plots\nplot(sol, idxs=(1, 2, 3), title=&quot;Lorenz attractor&quot;)"></div>
```

## Try `sjulia`

![](assets/julia-vm-oss.png){.absolute top="0" right="0" width="170"}

- Try `sjulia` (SubsetJuliaVM) on your machine!!!
- Install Rust
- Run the following commands:

```sh
$ git clone https://github.com/AtelierArith/julia-vm-oss.git
$ cd julia-vm-oss
$ ./scripts/sjulia_install.sh
$ sjulia examples/mandelbrot.jl
  0.40394 seconds
Mandelbrot Set (50x25):

                              .
                              . .
                              .+
                             ###+.
                        .   .####.
                       .#++#########....
                      ..##############.
            .        ..################..
             ...... .##################.
            .#######.###################
          ...##########################.
#####################################..
          ...##########################.
            .#######.###################
             ...... .##################.
            .        ..################..
                      ..##############.
                       .#++#########....
                        .   .####.
                             ###+.
                              .+
                              . .
                              .

```

## `juliars`

![](assets/julia-vm-oss.png){.absolute top="80" right="0" width="170"}

- A Julia-to-Rust transpiler (experimental)
- Unlike JuliaC, it does not require `libjulia`

```sh
$ cargo build --release -p subset_julia_vm \
    --features aot --bin juliars
$ target/release/juliars --minimal-prelude examples/mandelbrot.jl \
    --emit-binary target/mandelbrot_aot
$ target/mandelbrot_aot
  9.4e-5 seconds
Mandelbrot Set (50x25):

                              .
                              . .
                              .+
                             ###+.
                        .   .####.
                       .#++#########....
                      ..##############.
            .        ..################..
             ...... .##################.
            .#######.###################
          ...##########################.
#####################################..
          ...##########################.
            .#######.###################
             ...... .##################.
            .        ..################..
                      ..##############.
                       .#++#########....
                        .   .####.
                             ###+.
                              .+
                              . .
                              .
```

## Performance: Julia vs. SubsetJuliaVM {.smaller}

Mandelbrot set benchmark (Apple Silicon macOS, measured 2026-07):

::: {.columns}
::: {.column width="55%"}
```julia
function mandel_point(c::ComplexF64,
                      maxiter::Int64)::Int64
    z = 0.0 + 0.0im
    for k in 1:maxiter
        abs2(z) > 4.0 && return k - 1
        z = z * z + c
    end
    return maxiter
end

function mandel_count(width, height, maxiter)
    total = 0
    for y in 1:height
        ci = -1.2 + 2.4 * (y - 1) / (height - 1)
        for x in 1:width
            cr = -2.0 + 3.0 * (x - 1) / (width - 1)
            total += mandel_point(cr + ci * im, maxiter)
        end
    end
    total
end

mandel_count(1500, 1500, 500)  # same source for all implementations
```
:::
::: {.column width="45%"}
| Runtime | Time |
|-----------|-----:|
| Upstream Julia | 0.50 s |
| `juliars` (AoT) | 0.52 s |
| sjulia **VM** | 4.0 s |
| Python 3.14 | 20.8 s |

- AoT is **about as fast as upstream Julia**
:::
:::

## Interested in the Android app?

- Coming soon — we are looking for testers.
- Send an email to `s.terasaki@atelier-arith.jp`.
![](assets/google_play.png)

---

# Thank you!!!
